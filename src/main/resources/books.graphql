
schema{
    query:Query
}

type Query{
    allBooks: [Book]
    book(id:String):Book
}

type Book{
    isn: String
    title: String
    publisher: String
    authers: [String]
    publishedDate: String
}